<html>
  <body>
    <h1>{{ name }}</h1>
    <p><strong>Birthdate:</strong> {{ birthdate }}</p>
    <p><strong>Birthplace:</strong> {{ birthplace }}</p>
    <p><strong>Deathdate:</strong> {{ deathdate }}</p>
    <p><strong>Deathplace:</strong> {{ deathplace }}</p>
    <p><strong>Nationality:</strong> {{ nationality }}</p>
    <p><strong>Occupation:</strong> {{ occupation }}</p>

    <h2>Known for:</h2>
    <ul>
      {{#each known_for}}
      <li>{{ this }}</li>
      {{/each}}
    </ul>

    <h2>Education:</h2>
    <ul>
      {{#each education}}
      <li>
        {{ degree }}
        in
        {{ field }}
        from
        {{ institution }}
        ({{ year }})
      </li>
      {{/each}}
    </ul>

    <h2>Notable Publications:</h2>
    <ul>
      {{#each notable_publications}}
      <li>{{ title }} ({{ year }}) - {{ publisher }}</li>
      {{/each}}
    </ul>

    <h2>Partners:</h2>
    <ul>
      {{#each partners}}
      <li>{{ name }} ({{ relationship }}, {{ years }})</li>
      {{/each}}
    </ul>

    <h2>Awards:</h2>
    <ul>
      {{#each awards}}
      <li>{{ title }} ({{ year }}) - {{ description }}</li>
      {{/each}}
    </ul>

    <h2>Influences:</h2>
    <ul>
      {{#each influences}}
      <li>{{ this }}</li>
      {{/each}}
    </ul>

    <h2>Influenced:</h2>
    <ul>
      {{#each influenced}}
      <li>{{ this }}</li>
      {{/each}}
    </ul>

    <h2>Quotes:</h2>
    <ul>
      {{#each quotes}}
      <li>{{ this }}</li>
      {{/each}}
    </ul>

    <h2>Major Discoveries:</h2>
    <ul>
      {{#each major_discoveries}}
      <li>{{ title }} ({{ year }}) - {{ description }}</li>
      {{/each}}
    </ul>

    <h2>Contributions:</h2>
    <p>
      {{ contributions.description }} ({{
        contributions.year
      }}) in the field of {{ contributions.field }}
    </p>
  </body>
</html>
{{!-- 
    Цей код містить лише те, що буде виводитись в {{{body
}}} в layout/default.hbs Адже наш layout/default.hbs має вже
структуру HTML --}}
